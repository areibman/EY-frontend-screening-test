import React from 'react';

const Weather = () => {
    // TODO: Use any public API to fetch the weather in New York City
    const weather = "######"
    
    return <h2>The weather is {weather}</h2>
}

export default Weather